<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mansi.mansiabhinav_mapd711_onlinestore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".StoreActivity" />
        <activity android:name=".WomenStoreActivity" />
        <activity android:name=".MenStoreActivity" />
        <activity android:name=".KidsStoreActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ShowOrdersActivity" />
        <activity android:name=".ShowOrderByUserActivity" />
        <activity android:name=".UpdateOrderActivity"></activity>
    </application>

</manifest>